[project]
name = "lateral-sim"
version = "0.1.0"
description = "Lateral-move irrigator simulator for the lateral water-map app"
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.3.1",
]

[project.scripts]
doover-app-run = "main:main"
